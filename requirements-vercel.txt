# Lightweight requirements for Vercel deployment
# Removes heavy ML dependencies (PyTorch, CUDA libraries) to avoid OOM errors
# Uses OpenAI API for embeddings (recommended for production)

setuptools>=68.0.0
fastapi>=0.115.0
uvicorn[standard]==0.24.0
pydantic>=2.9.0
numpy>=1.26.0
scikit-learn>=1.5.0
beautifulsoup4==4.12.2
requests==2.31.0
openai>=1.40.0
python-multipart==0.0.6
python-dotenv==1.0.0
pandas>=2.2.0

# Note: sentence-transformers removed to reduce build size (~2GB+ savings)
# The code will use OpenAI API (if OPENAI_API_KEY is set) or fallback to random embeddings
# For local development with sentence-transformers, use requirements.txt

